# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GPS Member Store' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GPS Member Store

  pod 'AWSS3'
  pod 'AWSCognito'
  pod 'AWSCore'

  target 'GPS Member StoreTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GPS Member StoreUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
